import { PageShell } from "@/components/page-shell";

export default function TasksPage() {
  return (
    <PageShell
      title="Tasks"
      description="Placeholder. Tasks CRUD arrives in PHASE 07."
    />
  );
}
