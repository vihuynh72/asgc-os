import { PageShell } from "@/components/page-shell";

export default function MeetingsPage() {
  return (
    <PageShell
      title="Meetings"
      description="Placeholder. Meetings module starts in PHASE 21."
    />
  );
}
