import { PageShell } from "@/components/page-shell";

export default function AdminPage() {
  return (
    <PageShell
      title="Admin"
      description="Placeholder. Role assignments, config editors, and audit review come in later phases."
    />
  );
}
